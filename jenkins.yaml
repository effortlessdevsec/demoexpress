pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                sh 'git  https://github.com/effortlessdevsec/demoexpress.git'
            }
        }
        
}
}
